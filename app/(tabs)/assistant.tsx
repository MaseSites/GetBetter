import { AssistantView } from '@/features/assistant/AssistantView';

/**
 * Der mittlere Tab. Der Assistent ist eingebaut — nichts zu installieren,
 * nichts freizuschalten. Er verwaltet quer ueber die installierten Module.
 */
export default function AssistantTab() {
  return <AssistantView />;
}
