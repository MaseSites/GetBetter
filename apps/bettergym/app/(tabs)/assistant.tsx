import { AssistantView } from '@/features/assistant/AssistantView';

/** Der mittlere Tab — derselbe Assistent wie in GetBetter, wie im Entwurf. */
export default function AssistantTab() {
  return <AssistantView />;
}
