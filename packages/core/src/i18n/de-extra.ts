/**
 * Texte, die bisher am Code vorbei liefen. Teil von `de` — die Schluessel
 * landen dort per Spread. Zurzeit leer: alles Sichtbare hatte schon einen
 * Schluessel oder kommt aus `de-modules.ts`.
 */
export const deExtra = {} as const;
