import { ChatsView } from '@/features/ai/ChatsView';

/** BetterAi ist eine App fuer genau eine Sache — die Gespraeche sind die Startseite. */
export function AiHomeScreen() {
  return <ChatsView />;
}
